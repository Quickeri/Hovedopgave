mutation 
  updateNote (
    $userId: ID!
    $noteId: ID!
    $newTitle: String!
    $newDescription: String!
  ) {
    updateNote(
        userId: $userId
        noteId: $noteId
        newTitle: $newTitle
        newDescription: $newDescription
    ) {
    title
    description
    }
}